# Required: ts, repo, run_id, step, commands[] (cmd, exit_code), git{} (branch, head, dirty, diff_stat). One JSON object per reviewer run.
{"_schema":"ai-workflow-evidence.v1","note":"Append one JSON object per reviewer run. Keep it factual: commands + exit codes + git snapshot (branch/head/dirty/diff_stat)."}
